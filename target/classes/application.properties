# Server
server.port=8080

# Azure Key Vault Configuration (Desabilitado para desenvolvimento local)
# spring.cloud.azure.keyvault.secret.property-sources[0].endpoint=https://${AZURE_KEYVAULT_NAME}.vault.azure.net/
# spring.cloud.azure.keyvault.secret.property-sources[0].name=azure-keyvault

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=santos09
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT - Chave secreta de pelo menos 32 caracteres
jwt.secret=sua_chave_secreta_muito_longa_e_segura_aqui_com_mais_de_32_caracteres_para_jwt
jwt.expiration=86400000

# Swagger / OpenAPI - UI em /swagger-ui.html e /swagger-ui/index.html
springdoc.swagger-ui.path=/swagger-ui.html
